apiVersion: apps/v1
kind: Deployment
metadata:
  name: instagram-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: insta
  template:
    metadata:
      labels:
        app: insta
    spec:
      containers:
      - name: instagram-cont
        image: rajeshtutta123/instagram:latest
        ports:
        - containerPort: 5000
